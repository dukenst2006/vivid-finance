.Panel
  box-shadow : $box_shadow_normal
  +has(header)
    padding: 1rem
    +variant(primary)
      background-color: $primary

  +has(title)
    font-size: 1rem
    margin: 0
    padding: 0
    +variant(primary)
      color: $white

  +has(body)
    background-color: $white
    padding: 1rem