// Imports
@require "./../../../../assets/stylus/helpers/spa-imports.styl"

.user
  +has(image)
    +variant(sidebar)
      border-radius: 100%
      display: inline-block
      height: 6rem
      object-fit: cover
      vertical-align: middle
      width: 6rem
